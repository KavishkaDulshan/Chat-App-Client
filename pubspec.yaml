name: chat_client
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  socket_io_client: ^3.1.3
  google_fonts: ^6.3.3
  intl: ^0.20.2
  http: ^1.6.0
  flutter_secure_storage: ^10.0.0
  flutter_riverpod: ^3.0.3
  state_notifier: ^1.0.0
  image_picker: ^1.2.1
  dio: ^5.9.0
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  photo_view: ^0.15.0
  cryptography: ^2.7.0

  # ✅ UPDATE THIS LINE
  audioplayers: ^6.0.0
  path_provider: ^2.1.2
  permission_handler: ^11.3.0
  record: ^5.2.0 # Upgrade to 5.2.0 to fix platform conflicts

  # Local Database (Offline Message Cache)
  drift: ^2.22.1
  drift_flutter: ^0.2.4
  sqlite3_flutter_libs: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.22.1
  build_runner: ^2.4.0

flutter:
  uses-material-design: true

dependency_overrides:
  record_linux: ^1.0.0
